/* ICU Clinical Notes v4 frontend configuration.
 * Set API_URL to your deployed API proxy URL, NOT the Apps Script /exec URL.
 * Example: https://icu-notes-api.yourname.workers.dev
 */
window.ICU_CONFIG = Object.freeze({
  API_URL: 'PASTE-YOUR-API-PROXY-URL-HERE'
});
